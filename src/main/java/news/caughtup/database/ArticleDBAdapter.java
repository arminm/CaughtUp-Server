package news.caughtup.database;

import news.caughtup.model.Article;

public class ArticleDBAdapter extends DBAdapter {

    public static synchronized Article getArticle(String title) {
        // TODO: return the actual article
        return null;
    }

    public static synchronized void saveArticle(Article article) {
        // TODO: save to DB
    }

    public static synchronized void updateArticle(Article article) {
        // TODO: update article info in DB
    }

    public static synchronized void deleteArticle(String title) {
        // TODO: delete article from DB
    }
}
